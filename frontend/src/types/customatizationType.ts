export interface HeaderCustomizationType {
    bgColor: string;
    textColor: string;
    fontFamily: string;
    fontSize: number;
}

export interface DialogCustomizationType {
    bgColor: string;
    textColor: string;
    messageBg: string;
    borderColor: string;
    fontFamily: string;
    fontSize: number;
}